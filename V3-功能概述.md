# DSH Launcher V3 功能概述

> DSH Launcher 是 dsh 的桌面客户端:免装 Node、一键部署、单窗口内嵌 DSH 界面。
> 内核零改动 —— 直接运行官方 dsh,完整保留「一切皆插件」架构,与 CLI 共享 `DSH_HOME`,
> 已有会话 / API Key 直接生效。
>
> V3 的核心主题:**从"启动器"升级为"多实例管理中枢"** —— 一套客户端并行运行多套
> 独立的 DSH 环境,每个环境自由搭配不同的插件组合,配合整合包一键装机、插件市场与
> 插件矩阵,把插件生态的灵活性变成普通用户也能随手使用的能力。

---

## 一、V3 核心升级

### 1. 多实例体系(全新)

一套客户端内并行运行多个互不干扰的 DSH 实例:

- **独立配置**:每个实例拥有独立的插件配置(profile)与端口;给「写代码」「日常聊天」「跑测试」各建一个实例,切换实例 = 切换整套插件环境。
- **实例管理页**:实例网格卡片(状态灯 / profile / 端口 / 运行时长),支持新建、编辑、启停、重启、删除(至少保留一个,删除仅移除列表、不删磁盘数据)。
- **自动启动**:每个实例可单独设置随客户端启动自动运行。
- **外部实例采纳**:检测到外部手动启动的 dsh 实例时,自动识别为 `外部运行中` 状态,可从客户端直接接管停止。
- **会话说明**:所有实例共享同一份聊天记录,会话管理全局统一;注意**不要多个实例同时打开同一个对话**,并发操作同一会话会导致崩溃/数据损坏。

### 2. 推荐整合包:一键装机(全新)

不用再一个个挑插件 —— 内置官方精选整合包,一键下载、自动建实例、自动装齐:

| 整合包 | 内容 |
| --- | --- |
| 新手起步套装 | 11 个社区插件:图片问答与 OCR、内联图表/表单、@文件引用、消息批注、对话导入、桌面通知、长期编码记忆、余额峰谷监控、状态轮播、侧边栏工作台 |
| 社区精选增强包 | 27 个社区插件:会话右键菜单、插件市场/管理/守护/健康检查、超长上下文与云端记忆、文件改动审阅与检查点回滚、成本管控、自动折叠工具卡片、界面增强 |

- **进度透明**:下载过程显示整合包总进度条 + 每个插件的实时任务控制台。
- **部分失败不阻断**:个别插件装不上时,其余照常完成,结束后给出「未安装成功」清单(可一键复制),并可单独重试。
- **智能自愈**:遇到 pnpm 构建白名单(allowBuilds)缺失导致的安装失败,自动检测、自动补白名单并重试。
- **选择说明**:整合包由社区贡献挑选,只是一个起点 —— 装完后仍可在插件市场自由增配,单个插件也可独立更新/卸载。

### 3. 插件市场与插件管理(升级)

- **插件市场**:GitHub 插件主题搜索 + 分页 + 星级排序,仓库 README 内嵌预览,自动识别多子包仓库并让用户选择安装哪个子包,一键安装并归档到本地插件库。
- **插件管理页**:插件 × 实例矩阵(横轴实例、纵轴本地插件,一目了然看谁装了谁没装);自定义显示名与备注;单插件更新(`dsh plugin up`);批量删除;直装插件显示其安装 spec;peer 依赖不可满足时支持透传 pnpm 绕过参数。

### 4. 启动健壮性(升级)

- **智能启动超时**:不再用固定秒数判死 —— 只要进程还活着且持续输出日志,就判定"仍在启动",每 15 秒提示当前耗时;只有进程 120 秒无任何输出(真卡死)才判定超时。慢启动(源码版 + 几十个插件冷启动)不再被误报失败。
- **超时自愈**:即使被误判,端口一旦真正就绪,状态自动恢复为运行中。
- **残留进程清理**:启动时检测被遗留的 dsh 进程(端口占用但进程已死),自动清理后重启。
- **运行时链接层自愈**:启动前自动修复插件运行时解析链接(junction),失败只记日志、不阻塞启动。

### 5. 桌面体验升级

- **报错信息全部可复制**:所有报错 / 警告 / 失败清单支持一键复制按钮,控制台支持「复制全部」,日志与任务输出均可拖选复制。
- **内置说明问答**:「关于整合包」「什么是实例?」两个说明入口紧贴标题,点开即看 Q&A —— 整合包是什么、和实例的关系、出问题怎么办;实例的作用、会话共享与并发警告。
- **多实例合并日志控制台**:所有实例的日志按时间合并显示,每条带实例标签与配色,支持自动滚动 / 一键复制全部 / 清空。

---

## 二、全量功能一览

| 模块 | 功能 |
| --- | --- |
| 部署 | 免装 Node 一键部署;内置独立 Node + npm + pnpm + dsh,依赖版本与官方严格一致;内置 dsh 独立升级 |
| 运行 | 一键启动 / 停止 / 重启;单窗口内嵌 DSH 界面,不跳浏览器;全屏沉浸 + 悬浮球;外链打开系统浏览器 |
| 实例 | 多实例并行、独立 profile / 端口、自动启动、外部实例采纳、实例管理页 |
| 整合包 | 新手起步套装(11 插件)、社区精选增强包(27 插件),一键装机 + 进度 + 失败清单 + 自愈重试 |
| 插件 | 插件市场(GitHub 搜索 / README / 子包识别)、插件 × 实例矩阵、显示名与备注、更新 / 批量删除 / 本地库归档 |
| 监控 | 余额小部件(手动 + 5 分钟自动刷新)、实时日志、任务控制台、状态三态灯 |
| 桌面 | 系统托盘常驻 + 托盘状态灯、悬浮球、开屏动画、关闭至托盘 |
| 界面 | 中英双语切换(界面与主进程日志)、深色主题、多语言 Q&A 说明 |
| 故障 | 智能启动超时、端口冲突识别、残留进程清理、运行时链接自愈、报错一键复制 |

---

## 三、架构与兼容性

- **内核零改动**:直接运行官方 dsh 源码/内置发行版,不魔改内核,一切插件能力完整保留。
- **两种运行模式**:源码版(直接跑检查出的 harness 仓库,适合开发者)与内置版(便携 Node + 内置 dsh,免安装)。
- **共享配置**:与 dsh CLI 共享 `~/.dsh`(`DSH_HOME`),已有 API Key / 会话直接生效;实例的聊天记录全局共享。
- **Windows 原生体验**:NSIS 安装包、托盘、多屏子窗口、开机自启,全部走原生 API。

---

*本文档对应 V3 开发版功能状态;具体发布版本与更新记录以 Releases 页面为准。*
